{% for label, messages in app.flashes %}
	{% for message in messages %}
		<div class="d-flex align-items-center justify-center">
			<div class="alert alert-success d-flex align-items-center" role="alert" style="max-width: 500px;">
				{{message}}

				<p>
					xxxxxxxxxxxxxx</p>
			</div>
		</div>
	{% endfor %}
{% endfor %}
